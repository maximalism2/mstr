import React, { Component, PropTypes } from 'react';
import { TransitionMotion, spring } from 'react-motion';

class Loader extends Component {
  render() {
    return (

    );
  }
}

Loader.propTypes = {
  // showed: PropTypes.boolean.isRequired,
}
