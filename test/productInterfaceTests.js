'use strict';
var assert = require('assert');

var Product = require('../db/models/Products');
describe('Test to test', () => {
  it('test to test', () => {
    assert.equal(1, 1);
  });
  it('test to test', () => {
    assert.equal(2, 2);
  });
});
